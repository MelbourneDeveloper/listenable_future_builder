name: listenable_future_builder
description: A new Flutter package project.
version: 0.0.1-beta
homepage:

environment:
  sdk: '>=2.19.6 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  austerity: ^1.0.0
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter: